Creating a local instance of an environment in MySQL using Migration wizard, we will lose some table data, or we need the original data.

We will import the csv file in batches by the query provided above.
